import React from "react";

let initialState = {};

export const ErrorReducer = (state = initialState, action: any): any => {
    switch (action.type) {
        default:
            return state;
    }
}