import React from "react";

let initialState = {};


export const LoginReducer = (state = initialState, action: any): any => {
    switch (action.type) {
        default:
            return state;
    }
}