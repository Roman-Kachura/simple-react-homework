import React from "react";

let initialState = {};

export const RegistrationReducer = (state = initialState, action: any): any => {
    switch (action.type) {
        default:
            return state;
    }
}